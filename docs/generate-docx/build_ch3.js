const G = require("./gen");
const {
  h1, h2, h3, p, bold, italic, plain, bullet, figCaption, tblCaption,
  codeBlock, twoColCodeTable, dataTable, pageBreak,
} = G;

const ch3 = [
  h1("CHAPTER 3: CRYPTOGRAPHIC SUBSYSTEM ANALYSIS, PAYLOAD SERIALIZATION, & OPERATIONAL RESILIENCE"),

  h2("3.1 Introduction"),
  p("This chapter deconstructs the legacy crypto_vault.php cipher routine and documents its replacement with an AES-256-GCM Authenticated Encryption with Associated Data (AEAD) pipeline, implemented in src/Crypto/VaultCipher.php. It covers the cryptographic rationale for the mode change, the IV-management discipline the new mode requires, the exact wire-format serialization used to transport the IV, ciphertext, and authentication tag as one payload, the runtime exception-handling behaviour on tag-mismatch, the environment/secret-management controls (.env, .gitignore), and the PHPUnit automated verification suite."),

  h2("3.2 ECB Pattern Leakage vs. AEAD (AES-256-GCM) Internal Mechanics"),
  p("Section 1.6 established that ECB's block-independent, unauthenticated transformation lets identical plaintext blocks leak as identical ciphertext blocks. AES-256-GCM addresses both halves of this weakness structurally:"),
  bullet("Confidentiality without block-repetition leakage: GCM operates AES in counter (CTR) mode, XOR-ing the plaintext with a keystream generated by encrypting a per-block counter derived from the IV. Because the keystream block depends on the counter value (which increments every block) rather than on the plaintext itself, two identical plaintext blocks are XORed against two different keystream blocks and therefore almost never produce identical ciphertext — there is no structural mapping from plaintext equality to ciphertext equality, unlike ECB."),
  bullet("Authentication (the 'A' in AEAD): GCM additionally computes a GHASH-based Authentication Tag over the ciphertext (and any Associated Data) using a hash subkey derived from the AES key. This tag is a cryptographic checksum that only validates if every ciphertext bit, in its exact original order, is preserved — meaning GCM does not just hide data, it also detects any bit-level tampering or truncation at decryption time. ECB offers no equivalent: a legacy AES-128-ECB ciphertext can be truncated, reordered by block, or have blocks substituted from another ciphertext under the same key, and openssl_decrypt() will silently return a plausible-looking (but corrupted or attacker-controlled) plaintext with no error."),
  p("The live before/after evidence generated by demo/demo_ecb_vs_gcm.php (Figure 3) confirms both properties against the actual legacy cipher call and the actual VaultCipher implementation."),
  figCaption("Terminal capture — ECB ciphertext-block collision vs. GCM envelope divergence for identical plaintext"),
  codeBlock([
    "=== LEGACY: AES-128-ECB (crypto_vault.php, Hidden Flaw F) ===",
    "Plaintext block 1 == block 2: true",
    "Ciphertext block 1 (hex): 3d4ac7fb029cd158137eff97470619cf",
    "Ciphertext block 2 (hex): 3d4ac7fb029cd158137eff97470619cf",
    "Ciphertext blocks identical: TRUE (leak)  <-- pattern leakage",
    "",
    "=== SECURE: AES-256-GCM (VaultCipher, refactored) ===",
    "Envelope 1 (base64): xe3bTwypufmb26JshiqnYb5ocSkvieqavVmnp2FcCJWwU+kAEhiwYi+9fW0f3m5bF0i7oeZyeZmGjIA0",
    "Envelope 2 (base64): A91nPT21WRIv9aUa0Bx1G7k+OVRerb5LSSdeznmQVIrJoAW3dMlU2NB31nO3DQfUKTvJtwLi+33xNt6r",
    "Envelopes identical for identical plaintext: FALSE (secure)  <-- no pattern leakage (fresh IV each call)",
  ]),

  h2("3.3 Initialization Vector Dependency & Cryptographic Degradation"),
  p("GCM's security proof depends on one hard invariant: the pair (key, IV) must never repeat across two encryption operations. The IV seeds the initial counter block; if the same (key, IV) pair is reused for two different plaintexts, the two keystreams generated are identical, and XOR-ing the two ciphertexts together cancels the keystream entirely, exposing the XOR of the two plaintexts — from which both plaintexts can typically be recovered via frequency analysis or known-plaintext assumptions. Worse, GCM's authentication layer degrades catastrophically under IV reuse: an attacker who observes two ciphertexts under a repeated IV can algebraically recover the GHASH authentication subkey itself, which then permits forging valid authentication tags for arbitrary attacker-chosen ciphertext under that key — i.e., a single IV-reuse incident can compromise both confidentiality and the integrity guarantee for the entire key's lifetime, not just the two colliding messages."),
  p("VaultCipher::encrypt() (src/Crypto/VaultCipher.php) generates iv = random_bytes(12) on every single call using PHP's CSPRNG, so no IV is ever chosen by the caller, cached, or derived deterministically from data. With a 96-bit random IV, the probability of an accidental collision across N encryptions is governed by the birthday bound (~N^2 / 2^97); at realistic operational volumes (well under 2^32 messages per key) this probability is cryptographically negligible. This is why the class deliberately exposes no encrypt(plaintext, iv) overload — removing the possibility of a caller supplying (and potentially reusing) an IV is itself a defensive design decision, not an oversight."),

  h2("3.4 Low-Level Payload Serialization Sequence"),
  p("The refactored pipeline must transport three logically distinct fields — a 12-byte IV, a variable-length ciphertext, and a 16-byte authentication tag — as a single opaque string suitable for storage in a TEXT column, a JSON payload, or an HTTP body. VaultCipher packs them by fixed-offset concatenation rather than a delimiter, because a delimiter character could itself appear inside raw ciphertext bytes and corrupt parsing; fixed offsets make no assumption about ciphertext content."),
  figCaption("Structural data-flow diagram — VaultCipher envelope pack/unpack sequence"),
  codeBlock([
    "ENCRYPT (VaultCipher::encrypt)                    DECRYPT (VaultCipher::decrypt)",
    "--------------------------------                   --------------------------------",
    "1. iv       = random_bytes(12)                     1. raw = base64_decode(envelope)",
    "2. (ct,tag) = openssl_encrypt(                      2. iv  = substr(raw,  0, 12)",
    "                plaintext, 'aes-256-gcm',            3. tag = substr(raw, 12, 16)",
    "                key, iv, tag_out, '', 16)            4. ct  = substr(raw, 28)  // rest",
    "3. envelope = base64_encode(                        5. pt  = openssl_decrypt(",
    "                iv . tag . ct)                                    ct, 'aes-256-gcm',",
    "4. return envelope (string)                                        key, iv, tag)",
    "                                                     6. if (pt === false) throw",
    "                                                          VaultIntegrityException",
    "",
    "WIRE FORMAT:  base64( [ 12-byte IV ][ 16-byte TAG ][ N-byte CIPHERTEXT ] )",
    "                        offset 0     offset 12       offset 28 .. 28+N",
  ]),
  p("Placing the fixed-length IV and tag before the variable-length ciphertext means both boundaries are known constants (bytes [0,12) and [12,28)), so unpacking never needs to scan for a separator; the ciphertext is simply 'everything after byte 28'. The OPENSSL_RAW_DATA flag is used on both encrypt and decrypt so PHP does not additionally base64-encode the raw ciphertext internally before this envelope's own single base64 pass is applied — avoiding a redundant, wasteful double-encoding."),

  h2("3.5 Execution Flow on Authentication Tag Mismatch"),
  p("openssl_decrypt() does not throw on a GCM tag mismatch — following PHP's historical C-extension convention, it returns the boolean false, exactly as it would for several unrelated failure modes (e.g., an invalid key length). This is the RUNTIME TRAP flagged in the legacy file's own comment: code that upgrades to GCM but forgets to explicitly check for a false return will treat the boolean false as if it were a valid plaintext string — for instance, silently passing it into a downstream function expecting patient data, or logging it as 'successfully decrypted' output. In an unhandled scenario, this can propagate as type-confusion bugs, corrupted records displayed to clinicians, or, if false is coerced to an empty string context, a fail-open condition where tampered data is treated as trusted."),
  p("VaultCipher::decrypt() closes this gap explicitly: the false return is checked immediately and converted into a thrown VaultIntegrityException carrying a descriptive message. This changes the failure mode from an implicit, easily-missed falsy value into an explicit control-flow event that Zend's exception-propagation mechanism guarantees cannot be silently ignored — if the caller does not catch VaultIntegrityException, PHP unwinds the call stack and terminates the specific request with an uncaught-exception fatal error, rather than continuing execution with corrupted data. The demonstration endpoint in src/crypto_vault.php (handleDecrypt()) shows the correct, isolated failure state: the exception is caught at the transport boundary and converted into an HTTP 422 Unprocessable Entity response with a safe, generic error message — the request fails cleanly and in isolation, without crashing the PHP-FPM worker process or leaking implementation detail to the client."),

  h2("3.6 Environment Dependency Mapping & Secret Externalisation"),
  h3("3.6.1 Decoupling Secrets into .env"),
  p("Hidden Flaw G hardcoded $secret_key directly in crypto_vault.php. The refactor removes every literal secret from source: CRYPTO_VAULT_KEY_B64, and all DB_* credentials, are read at runtime from process environment variables populated by vlucas/phpdotenv from a .env file (src/bootstrap.php). This decoupling means the dependency graph for 'what does this code need to run' now terminates at a runtime configuration artefact rather than at a compiled/committed source file — a key can be rotated, or a different key used per-environment (development/staging/production), without touching a single line of PHP."),
  h3("3.6.2 .gitignore and the Attack Surface"),
  p(".gitignore in the project root excludes .env (while explicitly whitelisting .env.example, the placeholder template) from version control tracking. This structurally prevents the single most common secret-leak vector: a developer running git add . and committing real credentials into project history — history which, on a public or later-compromised repository, remains recoverable indefinitely even after the offending file is deleted in a later commit. By excluding .env at the tooling level rather than relying on developer discipline at commit time, the attack surface for credential leakage via source-control exposure is closed by default rather than by convention."),

  h2("3.7 Automated Verification — PHPUnit Test Suite"),
  p("tests/VaultCipherTest.php, tests/AuthServiceTest.php, and tests/PatientSearchServiceTest.php together assert three explicit runtime states required by the assessment brief: (i) an untampered cryptographic lifecycle round-trips correctly; (ii) a tampered-ciphertext execution path throws the expected AEAD exception rather than returning corrupted data; and (iii) credential hash integrity holds for Argon2id verification."),
  tblCaption("Key PHPUnit assertions mapped to the three required runtime states"),
  dataTable(
    ["Runtime state", "Test method", "Assertion mechanism"],
    [
      ["(i) Untampered lifecycle", "testEncryptDecryptRoundTripReturnsOriginalPlaintext", "assertSame($plaintext, $recovered) — no exception thrown"],
      ["(ii) Tampered ciphertext -> AEAD exception", "testTamperedCiphertextThrowsIntegrityException", "expectException(VaultIntegrityException::class) after flipping one ciphertext byte"],
      ["(ii) Malformed / truncated envelope", "testMalformedEnvelopeThrowsIntegrityException", "expectException(VaultIntegrityException::class) on a too-short envelope"],
      ["(iii) Credential hash integrity", "testHashThenVerifySucceedsForCorrectKey", "assertTrue($auth->verify(...)) against a freshly Argon2id-hashed key"],
      ["(iii) Boundary character-count", "testMultiByteInputIsBoundedByCharacterCountNotByteCount", "assertTrue($auth->isWithinBounds(...)) for a 256-character multi-byte string"],
    ],
    [30, 40, 46]
  ),
  p([bold("Distinguishing an anticipated security exception from a catastrophic crash. "), plain("PHPUnit's expectException() mechanism registers the expected exception class before the tested call executes, then asserts that exact class (or a subclass) was thrown during that call; if no exception is thrown, or a different exception/fatal error occurs, the assertion itself fails with a clear diagnostic rather than allowing the process to terminate uncontrolled. This is structurally different from an unhandled TypeError or Fatal Error bubbling out of the test runner: expectException() treats the security exception as a first-class, expected outcome of a correctly-functioning system boundary (\"reject tampered input\"), whereas an uncaught fatal error would instead indicate the code under test crashed for a reason the test was not designed to verify — PHPUnit reports the latter as an ERROR, not a PASS, making the two failure classes visually and semantically distinguishable in the test report.")]),
  p("The full transcript below is the actual terminal output produced by running ./vendor/bin/phpunit --testdox against this repository."),
  figCaption("Live terminal capture — ./vendor/bin/phpunit --testdox (14 tests, 20 assertions, all green)"),
  codeBlock([
    "PHPUnit 11.5.55 by Sebastian Bergmann and contributors.",
    "",
    "Runtime:       PHP 8.3.32",
    "Configuration: HealthVault-Secure/phpunit.xml",
    "",
    "..............                                                    14 / 14 (100%)",
    "",
    "Time: 00:00.042, Memory: 8.00 MB",
    "",
    "Auth Service (HealthVault\\Tests\\AuthService)",
    " ✔ Hash then verify succeeds for correct key",
    " ✔ Verify fails for incorrect key",
    " ✔ Multi byte input is bounded by character count not byte count",
    " ✔ Multi byte input exceeding character bound is rejected",
    " ✔ Legacy md 5 hash is flagged as needing rehash",
    "",
    "Patient Search Service (HealthVault\\Tests\\PatientSearchService)",
    " ✔ Search returns matching records for ordinary keyword",
    " ✔ Classic sql injection payload is treated as literal data",
    " ✔ Stacked query injection payload does not alter result set",
    "",
    "Vault Cipher (HealthVault\\Tests\\VaultCipher)",
    " ✔ Encrypt decrypt round trip returns original plaintext",
    " ✔ Identical plaintext produces different envelopes",
    " ✔ Envelope contains expected iv and tag lengths",
    " ✔ Tampered ciphertext throws integrity exception",
    " ✔ Malformed envelope throws integrity exception",
    " ✔ Rejects key that is not exactly 32 bytes after decoding",
    "",
    "OK (14 tests, 20 assertions)",
  ]),

  h2("3.8 Chapter Summary"),
  p("The cryptographic subsystem now provides confidentiality and integrity as a single AEAD guarantee, sources its key exclusively from an externalised, git-ignored environment file, and is continuously verified by an automated PHPUnit suite that explicitly exercises both the correct-operation and tampered-input code paths. Chapter 4 correlates this remediation with the ones in Chapters 1-2 into a single forensic dependency chain and closes with the architectural synthesis required for the final submission."),
  pageBreak(),
];

module.exports = { ch3 };
